services:
  wanderlog-postgres-db:
    image: postgres:15
    container_name: wanderlog-postgres-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Talha8293.
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    restart: always

  wanderlog-mongodb:
    image: mongo:6.0
    container_name: wanderlog-mongodb
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: adminpassword123
    ports:
      - "27017:27017"
    restart: always
    volumes:
      - mongo_data:/data/db

  wanderlog-mariadb:
    image: mariadb:10.11
    container_name: wanderlog-mariadb
    environment:
      MARIADB_ROOT_PASSWORD: rootpassword123
      MARIADB_DATABASE: okul_sistemi
    ports:
      - "3306:3306"
    restart: always
    volumes:
      - mariadb_data:/var/lib/mysql

  wanderlog-redis-cache:
    image: redis:alpine
    container_name: wanderlog-redis-cache
    ports:
      - "6379:6379"
    restart: always

  wanderlog-python-app:
    build: .
    container_name: wanderlog-python-app
    depends_on:
      - wanderlog-postgres-db
      - wanderlog-redis-cache
    restart: always

  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    ports:
      - "9000:9000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: always

  airflow-standalone:
    image: apache/airflow:2.7.0
    container_name: airflow-standalone
    restart: always
    environment:
      - AIRFLOW__CORE__EXECUTOR=SequentialExecutor
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
      - _PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-mysql redis
    ports:
      - "8080:8080"
    volumes:
      - ./dags:/opt/airflow/dags
      - ./logs:/opt/airflow/logs
      - ./plugins:/opt/airflow/plugins
    command: standalone

  elasticsearch:
    image: elasticsearch:8.10.2
    container_name: elasticsearch-lab
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    ports:
      - "9200:9200"
    volumes:
      - es_data:/usr/share/elasticsearch/data

  kibana:
    image: kibana:8.10.2
    container_name: kibana-lab
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    depends_on:
      - elasticsearch

  filebeat:
    image: elastic/filebeat:8.10.2
    container_name: filebeat-lab
    user: root # Journald okumak için root yetkisi zorunludur
    volumes:
      - ./filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - elasticsearch
      - kibana

volumes:
  es_data:
  mongo_data:
  mariadb_data:
